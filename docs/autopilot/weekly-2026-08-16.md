# PIXELORIA WEEKLY REPORT — 2026-08-16

> Rapport hebdomadaire de la Pixeloria AI Team.
> Cycle : 2026-08-09 → 2026-08-16 (7 jours).
> Auteur : CEO AI · synthèse des 6 pôles (Growth · Product · Engineering · Marketing · Business · Operations).
> Mode d'exécution : scheduled task, audit lecture-seule, aucun code modifié. 6 issues créées (#180-#185).

---

## Résumé exécutif

**Cycle exceptionnellement productif côté delivery** : les 18 issues du rapport précédent (#145-#164) sont **toutes fermées**, 13 PR mergées (#166-#179), backlog vidé à 100 %, aucune PR ouverte. Les livrables majeurs : Home FR complétée (Testimonials + AuditGratuit + WhyPixeloria + Process, #145), catalogue US wire complet (`lib/pricing-us.ts`, #146), fusion ContactForm FR/EN + hook Turnstile partagé (#162), fusion TestimonialForm FR/EN + Turnstile server-side sur `submit-testimonial` (#173), bump sharp 0.35.3 (#174), programme parrainage US via lead capture — plus de Stripe US (#155), positionnement US "remote-only Organization" tranché (#161), tests React Testing Library ajoutés (56 tests unitaires vs 20 la semaine dernière).

**Deux régressions P0 identifiées ce cycle et transformées en issues** :
- **#180 — NAP incohérent Home FR** : `components/sections/Contact.tsx:25` affiche `07 86 12 53 13` alors que `tel:` / WhatsApp / JsonLd / Footer utilisent tous `+33 6 01 77 76 33`. Bug introduit par le commit `2c4b4d2` qui a réaligné le `tel:` sur 25 fichiers mais a oublié le `<span>` visible.
- **#181 — `/en/parrainage` follow-up #155** : le formulaire propose Starter $499 / Growth $899 / Care $79-mo en USD, mais **34 occurrences `€/EUR/inc. VAT`** subsistent dans `app/en/parrainage/page.tsx` (~31) et `components/parrainage/ParrainageFaqEn.tsx` (~3). L'issue #155 a été fermée `state_reason=completed` alors que son propre critère d'acceptation (`grep "€" ⇒ 0 hit`) n'est pas satisfait. Le commit `b1af6cb` reconnaît explicitement le scope-cut (« Deliberately NOT touched »).

**Quatre P1 nouveaux** transformés en issues #182-#185 : UTM tracking silencieusement strip par Zod (attribution acquisition impossible), `ReferralLeadFormEn` sans event GTM/Vercel Analytics (nouveau tunnel US non mesuré), 2 scripts (`create-stripe-referral-links.ts`, `setup-notion-db.ts`) référencent encore l'alias mort `site-vitrine`, sitemap `lastModified: new Date()` bruyant sur les 79 URLs.

Sur les dettes persistantes, aucun mouvement : Next.js 14 → 15/16 (5 HIGH CVE), CSP `unsafe-inline` (attend script hash SHA-256 build-time), `.github/` sans templates/CODEOWNERS/dependabot, `viewport.themeColor` absent, `humans.txt` absent, `docs/autopilot/experiments.md` toujours vide (3e cycle consécutif).

## Scores

Barème d'estimation qualitatif — aucun accès analytics live dans ce cycle (limite documentée depuis 2026-07-27). Delta calculé vs 2026-08-09.

- **Business** : **68/100** (+6) — pricing FR+US source unique, backlog Business 100 % fermé, Testimonials pipeline branché
- **SEO** : **76/100** (+2) — BreadcrumbList étendu (#149), h1 corrigés (#160), hreflang stabilisés (#158, #159)
- **GEO** : **62/100** (+1) — hreflang self-refs OK, mais `llms.txt` toujours à 63/120 lignes, `humans.txt` toujours absent
- **AI Citation** : **55/100** (=) — aucune source live pour mesurer, `llms.txt` +6 lignes vs cycle précédent
- **Conversion (CRO)** : **60/100** (+5) — Home FR étoffée, ContactForm unifié, TunnelForm defaults 0/0 — **pénalité #180/#181**
- **UX** : **66/100** (+4) — Home FR structure claire, aria-invalid dynamique EN ajouté — **pénalité #180**
- **Performance** : **74/100** (=) — build OK 90 routes 87 static ○ ratio préservé, non mesuré Lighthouse
- **Brand / Trust** : **52/100** (+4) — Home FR +3 sections trust, mais 1 seul témoignage textuel FR fallback
- **Accessibilité** : **76/100** (−2) — a11y CI branché (job axe-core, PR #167) mais couverture toujours 6 pages, `themeColor` absent, tap-targets 38px persistent
- **Sécurité** : **84/100** (−2) — sharp 0.35.3 + Turnstile généralisé, mais 5 HIGH CVE Next.js/postcss persistants et CSP `unsafe-inline`
- **Qualité des données (CRM/Notion)** : **50/100** (−5) — Notion propre mais **UTM tracking cassé Zod-side** (nouveau finding P1)

## Évolution de la semaine

| KPI | Valeur | Note |
|---|---|---|
| Trafic organique | non mesuré | GSC / Vercel Analytics API toujours non branchés — voir `.claude/memory/activeContext.md` |
| Impressions | non mesuré | idem |
| Clics | non mesuré | idem |
| Leads | non mesuré | Notion accessible mais pas exposé au pipeline AutoPilot |
| Taux de conversion | non mesuré | idem |
| Positions Google | non mesuré | GSC non branché |
| Citations IA (ChatGPT / Claude / Perplexity) | non mesuré | aucun outil de monitoring branché |
| Performances LCP / CLS / INP | non mesuré | Lighthouse à lancer en prod |
| Avis Google | non mesuré | composant `GoogleReviews` dynamique en prod, non snapshotté ici |
| Backlinks | non mesuré | aucun outil de monitoring branché |
| Pages indexées | **79 URLs** (stable) | sitemap inchangé côté cardinalité — 2 pages parrainage US existantes toujours indexées |
| Nouvelles réalisations | +0 | 4 études de cas FR, stable (2 concepts US, stable) |
| Articles publiés | +0 | blog FR toujours vide, `/en/resources/` = 3 guides (stable) |
| Temps de chargement moyen | non mesuré | Lighthouse à programmer |
| Commits sur `main` (7 j) | **+16** (hors merges) | 13 PR mergées (#166 → #179) |
| PR mergées (7 j) | **+13** (#166-#179) | ×13 vs cycle précédent |
| PR ouvertes | **0** | −1 (#153 mergée) |
| Issues ouvertes (backlog avant audit) | **18 → 0** puis **0 → 6** (post-audit #180-#185) | −18 backlog vidé, +6 nouveaux findings ce cycle |
| Issues fermées (backlog #145-#164) | **18 / 18** | **100 % du backlog du rapport précédent** |
| Tests unitaires Vitest | **56 tests / 9 fichiers** (+36 vs 20) | RTL ajouté : `ContactForm.test.tsx` 8 tests, `TestimonialForm.test.tsx` 3 tests |
| Tests e2e Playwright | ~60 tests / 7 fichiers | +17 vs ~43 précédent |
| Job CI a11y | **branché** (axe-core, 6 pages) | nouveau via PR #167 — 5e job CI |
| Build routes | **90 routes** (87 static ○, 3 dynamic ƒ) | ratio préservé, `/en/parrainage` reste ○ 5.53 kB |
| `npm audit` | **5 HIGH** restants (next, postcss transitif) | stable — résolution = migration Next 14→16 breaking |

## Actions réalisées

| PR | Titre | Impact |
|---|---|---|
| #166 (mergée 2026-08-11) | Daily audit 2026-08-10 — security, SEO, CRO | Améliorations mineures avant grand nettoyage |
| #167 (mergée 2026-08-12) | Resolve 14/18 backlog issues from PIXELORIA WEEKLY REPORT 2026-08-09 | Vague 1 : #147/#148/#149 (partiel)/#151/#154/#156/#157/#158/#159/#160/#161/#163/#164 + a11y color-contrast |
| #168 (mergée 2026-08-12) | Close remaining 2026-08-09 backlog (#146, #149, #162 partiel, #150) | Vague 2 : `lib/pricing-us.ts` complet, BreadcrumbList étendu, TestimonialForm fusionné, #150 réévalué |
| #169 (mergée 2026-08-12) | Complete Home FR with Testimonials + AuditGratuit, fix a11y bug | Fix #145 — Home FR complète (8 sections) + fix a11y étoiles Testimonials |
| #170 (mergée 2026-08-12) | Reflect final state after 2026-08-09 backlog closure | Memory bank alignée |
| #171 (mergée 2026-08-12) | Fix real bugs on ContactForm/EN without merging | #171 : faux succès réseau FR corrigé + aria-invalid dynamique EN |
| #172 (mergée 2026-08-12) | Update phone number and WhatsApp link site-wide (FR + US) | Réalignement `tel:` sur 25 fichiers — **oublie le `<span>` visible de `Contact.tsx:25` → régression P0 #180 introduite** |
| #173 (mergée 2026-08-12) | Verify Turnstile server-side on /api/submit-testimonial | Protection asymétrique corrigée — `submit-testimonial` désormais aussi robuste que `submit-lead` |
| #174 (mergée 2026-08-12) | Bump sharp to 0.35.3, npm audit fix, refresh memory bank | GHSA-f88m-g3jw-g9cj (libvips) résolu, + nanoid/brace-expansion/glob/js-yaml/fast-uri (transitifs) |
| #175 (mergée 2026-08-12) | Replace stale dark background on .section-dark with light theme color | Nettoyage reliquats dark theme |
| #176 (mergée 2026-08-13) | Confirm remote-only US positioning, merge ContactForm/EN | **Décision produit tranchée** : US = remote-only Organization (bloqueur #161 fermé). ContactForm FR/EN fusionné. |
| #177 (mergée 2026-08-13) | Address Codex review findings on merged ContactForm | a11y + i18n polish post-review |
| #178 (mergée 2026-08-13) | Add React Testing Library, cover ContactForm/TestimonialForm | +11 tests unitaires composants React (jsdom + `@testing-library/react`) |
| #179 (mergée 2026-08-13) | US referral program via lead capture, no Stripe needed | **Décision produit tranchée** : US referral = lead capture, no Stripe. Form USD, mais **marketing copy laissé en EUR → régression P0 #181** |

## Résultats mesurés

**Structurels (mesurables au repo, non live) :**
- Backlog GitHub : **18 → 0** issues ouvertes après clôture (puis +6 ce cycle = #180-#185).
- Tests unitaires : **20 → 56** (+36 tests, +11 fichiers React composants).
- Job CI a11y : **absent → présent** (axe-core sur 6 pages en CI depuis PR #167).
- CVE HIGH : **~21 → 5** (via `npm audit fix` sur sharp/nanoid/brace-expansion/glob/js-yaml/fast-uri). Reliquat = Next.js 14 (breaking, à traiter séparément).
- Fichiers dupliqués supprimés : `ContactFormEn.tsx`, `TestimonialFormEn.tsx` → source unique `components/forms/ContactForm.tsx` + `components/forms/TestimonialForm.tsx`.

**KPI live** : aucune mesure disponible (limite persistante — GSC / Vercel Analytics / GBP Insights toujours non branchés).

## Actions sans résultat suffisant

- **`docs/autopilot/experiments.md`** : toujours vide (3e cycle). Aucun cycle observer→apprendre AutoPilot capitalisé — les 2 tests A/B proposés au cycle précédent (retirer `required` sur `tel` du ContactForm FR, retirer step 2 du TunnelForm) n'ont ni été lancés ni été notés comme hypothèses.
- **`docs/autopilot/decisions.md`** : 2 décisions stratégiques prises ce cycle (US remote-only Organization via #176, US referral via lead capture via #179) **non capitalisées** dans le journal — resteront à re-litiger au prochain cycle si non écrites.
- **Aucune source analytics live branchée** : `.claude/memory/activeContext.md:59` — cycle 4 sans progression sur ce point, tout KPI reste "non mesuré".
- **`.github/` hygiène** : aucun template PR/Issue, pas de CODEOWNERS, pas de dependabot.yml — point ouvert 2026-08-09 non traité pour le 2e cycle consécutif.

## Régressions

- **#180 (P0) — NAP incohérent Home FR Contact** : introduit par PR #172 (fix contact site-wide) qui a réaligné le `tel:` sur 25 fichiers mais a oublié `Contact.tsx:25` `<span>07 86 12 53 13</span>`. Régression fonctionnelle réelle.
- **#181 (P0) — `/en/parrainage` marketing body en EUR** : introduit par PR #179 (feat #155). Régression fonctionnelle du finding — l'issue #155 fermée en `completed` alors que son critère d'acceptation `grep "€|inc\. VAT|EUR" ⇒ 0` échoue à 34 hits. Le commit `b1af6cb` documente le scope-cut mais aucune issue de suivi n'a été ouverte — #181 comble ce trou.
- **Régression de processus** : #155 fermée sans satisfaire son critère d'acceptation. Signal pour formaliser une règle « une issue ne se ferme que si son AC passe » côté `github-guardian` (point ouvert).
- Aucune régression détectée sur les axes sécurité API, hreflang, catalogue, build, tests.

---

## Opportunités

### Créer une page — Articles piliers ressources FR
**Sujets** : « Combien coûte un site internet pour un artisan en 2026 ? », « Wix ou agence : comparatif honnête pour un artisan », « Site vitrine vs one page pour une TPE ». *(Recommandé par Pôle Marketing Intelligence — inchangé depuis 08-09.)*
**Impact estimé** : Élevé — capture de longue traîne SEO/GEO, éligible AI Overview FR. Impact chiffré : non mesuré (pas d'historique).

### Créer une FAQ — Compléter la FAQ FR
Porter les 5 questions manquantes de `/en/faq` (12 questions) vers `components/sections/faq-data.ts` (actuellement 7). *(Recommandé par Pôle Marketing — inchangé.)*
**Impact estimé** : Moyen — enrichissement `FAQPage` schema.

### Créer une étude de cas — Enrichir les 4 études de cas FR avec verbatim + 1 chiffre
Demander à Fabien 1 phrase textuelle par client + 1 métrique (même approximative). Ajouter section « Ce qu'en dit le client » sous chaque étude. *(Recommandé par Pôle Marketing + Business.)*
**Impact estimé** : Élevé — Trust Score + preuves sociales. Contrainte humaine : accord client.

### Créer une page US — 2-3 vraies études de cas US
`app/en/case-studies/page.tsx` liste toujours uniquement 2 « concept demos ». *(Recommandé par Pôle Growth + Business + Marketing — inchangé.)*
**Impact estimé** : Élevé pour Trust US.

### Créer une landing sectorielle FR — Restaurant et Conciergerie
Deux verticales représentées par 3 études de cas (Escalée Goûts, Lenora, QIT) sans landing dédiée. *(Recommandé par Pôle Marketing.)*
**Impact estimé** : Moyen-fort — capitalise sur références client existantes.

### Ajouter FAQ + `FAQPage` schema sur les 4 landings piliers FR
3-5 Q/A par landing : `/creation-site-internet-artisan`, `/creation-site-internet-tpe-pme`, `/refonte-site-internet`, `/maintenance-site-web`. Modèle : `app/en/hvac-websites/page.tsx:76-97`.
**Impact estimé** : Moyen-fort — GEO/AI Overview FR sur pages commerciales.

### Ajouter `Service` + `FAQPage` JSON-LD sur `/parrainage` FR et `/en/parrainage` EN
Actuellement 0 schema sur les deux pages. *(Recommandé par Pôle Growth — nouveau.)*
**Impact estimé** : Moyen — rich result FAQ + qualification LLM du programme. Prérequis : #181 doit passer avant (ne pas figer les EUR dans le schema US).

### Enrichir `public/llms.txt` (63 → ~120 lignes)
Ajouter FAQ synthétique, verticales US, études de cas, zones geo, GBP URL. *(Recommandé par Pôle Marketing + Growth.)*
**Impact estimé** : Moyen — portée LLM directe.

### Créer `public/humans.txt`
Complète `llms.txt` avec E-E-A-T biographique. *(Inchangé.)*
**Impact estimé** : Faible-moyen — polish LLM. Effort XS.

### Ajouter URL Google Business Profile au `sameAs` JsonLd + `llms.txt`
URL profil GBP absente de `components/JsonLd.tsx:53` (sameAs = X, Instagram, LinkedIn, Facebook uniquement). *(Nouveau — Pôle Marketing.)*
**Impact estimé** : Moyen — signal Local SEO/Knowledge Graph.

### Étoffer le fallback `Testimonials.tsx` à 3 témoignages
Éviter que Home FR affiche 1 seule card si Notion vide. 2 verbatim tirés des relations clients existantes (avec consentement).
**Impact estimé** : Faible-moyen — polish Trust.

### Ajouter section « Vs alternatives » sur `/tarifs` (FR) et `/en/pricing` (US)
Pixeloria vs Simplébo/Wix (FR), vs Housecall Pro/DIY builder (US). *(Nouveau — Pôle Business.)*
**Impact estimé** : Moyen-fort — capture les visiteurs en phase de comparaison active.

### Nettoyer `.section-dark` (soit refactor, soit suppression des 4 usages `/en/*`)
Reliquat du dark theme non couvert par PR #175 sur `/en/pricing`, `/en/about`, `/en/reviews`, `/en/page.tsx`. *(Nouveau — Pôle Product.)*

### Test A/B — Retirer `required` sur `tel` du ContactForm FR
*(Recommandé par Pôle Product — inchangé.)*
**Impact estimé** : Moyen-fort — hypothèse baisse abandon formulaire.

### Test A/B — Retirer step 2 (visiteurs/leads) du TunnelForm pour offre 199 €
*(Recommandé par Pôle Product + Business — inchangé.)*
**Impact estimé** : Moyen — hausse taux complétion tunnel.

---

## Top 5 des priorités (semaine 2026-08-16 → 2026-08-23)

1. **#180 — Fixer le NAP Home FR Contact** (P0 · CRO/UX/SEO local). *Décision produit préalable : lequel des deux numéros est le vrai ?  Effort XS post-décision. Bug visible sur la page la plus vue du site.*
2. **#181 — Refonte `/en/parrainage` marketing body en USD** (P0 · Business/Marketing US). *Décision produit préalable : confirmer montants récompenses publics ($250/$450/$40 ?) + vocabulaire. Effort M (~3-4 h).* Débloque la conversion référence US.
3. **#182 — UTM tracking Zod + Notion** (P1 · Data/CRM). *Effort S (~1 h). Débloque toute attribution acquisition et respecte enfin la promesse marketing de `local-seo.ts:158` / `local-seo-en.ts:142`.*
4. **#183 — GTM/Vercel Analytics sur `ReferralLeadFormEn`** (P1 · Analytics). *Effort XS (2 imports + 2 lignes). Pré-requis pour mesurer l'impact du programme parrainage US.*
5. **#184 — Purger `site-vitrine` de `scripts/*.ts`** (P1 · Data). *Effort XS. Neutralise une dette dormante avant qu'elle ne casse un futur onboarding.*

Bonus si temps : **#185 — Sitemap `lastModified: new Date()`** (P1 · SEO). *Effort S (option A LAST_MOD statique) ou M (option B git log build-time).*

## Décisions humaines nécessaires

1. **Numéro de téléphone canonique** (bloque #180) : `+33 6 01 77 76 33` (Footer/JsonLd/WhatsApp/tel: sur 25 fichiers) ou `+33 7 86 12 53 13` (display Contact home) ? Une seule ligne visible partout.
2. **Montants récompenses parrainage US publics** (bloque #181) : afficher $250/$450/$40 (~50% du prix, transparent, cohérent avec le modèle FR) ou garder confidentiels ? Sous-choix : intitulé « Refer Pixeloria and earn up to $250 + $40/month » à valider.
3. **Programme parrainage US — position produit** : maintenir `/en/parrainage` actif après refonte USD, OU rétrograder temporairement (retirer du sitemap/nav, `noindex`) le temps de stabiliser le modèle ?
4. **Granularité UTM Notion** (bloque #182) : 5 colonnes rich_text séparées (segmentation fine) OU 1 colonne texte concat (simple, moins segmentable) ?
5. **Branchement d'au moins une source analytics live** (GSC ou Vercel Analytics API) — cycle 4 sans progression. Sans cet unlock, tout score Business/SEO/GEO/CRO reste qualitatif.
6. **Politique de fermeture d'issue** : #155 a été clôturée `completed` sans satisfaire son AC. Formaliser une règle « une issue ne se ferme que si son AC passe » côté `github-guardian` ? À défaut, refléter au moins le scope-cut dans le `state_reason` (`not planned` + nouvelle issue) au lieu de `completed`.
7. **Décisions produit ContactForm FR restantes** (héritées de #162) : téléphone FR requis ou optionnel ? Champ « Company » à ajouter côté FR (déjà présent EN) ? Bloque la parité complète FR/EN.

## Plan de la semaine suivante

**Semaine 2026-08-16 → 2026-08-23** — objectifs :
- **Jour 1** : décision humaine sur les 3 points bloquants #180/#181/#182 (numéro, montants US, granularité UTM). Fix #180 dans la foulée (XS post-décision).
- **Jour 2-3** : fix #181 — refonte complète `/en/parrainage` marketing body + FAQ EN + metadata SEO en USD. PR review + AC strict (`grep € ⇒ 0`).
- **Jour 3-4** : #182 (UTM Zod+Notion) + #183 (GTM ReferralLeadFormEn) — batchables ensemble (même thème Data/Analytics).
- **Jour 5** : #184 (scripts site-vitrine) + #185 (sitemap lastModified) — quick wins.
- **Jour 6-7** : commencer 1 des 3 articles piliers FR ("Combien coûte un site pour un artisan en 2026") — capture longue traîne prioritaire.

**Cycle observer→apprendre AutoPilot** : au minimum une entrée dans `docs/autopilot/experiments.md` d'ici la fin de semaine (hypothèse : après fix #180+#181, mesurer le delta lead form parrainage US). Documenter aussi les 2 décisions produit prises ce cycle dans `docs/autopilot/decisions.md`.

**Weekly report suivant** : 2026-08-23 — mêmes 6 pôles, focus sur delta backlog #180-#185 + éventuel branchement analytics live.

---

## Points ouverts (non bloquants, à documenter)

**Dette technique persistante (héritée) :**
- **Next.js 14 → 15/16** : 5 HIGH CVE restants (2 sur next : SSRF WebSocket GHSA-c4j6, DoS Server Components, i18n bypass, XSS beforeInteractive, cache poisoning, SSRF Server Actions, disclosure Server Functions ; 2 sur postcss transitif : XSS Stringify + Path Traversal). Résolution = `next@16.3.1` breaking. **Aucun nouveau CVE cette semaine.**
- **CSP `'unsafe-inline'`** (`next.config.mjs:33-35`) : attendu tant que le hash SHA-256 build-time (documenté sur issue #150 fermée) n'est pas implémenté. Le chemin technique reste : allowlist par hash SHA-256 calculé au build sur les JSON-LD statiques, compatible avec les ~90 routes static.
- **A11y coverage 6 pages** : `tests/a11y/accessibility.spec.ts` scanne toujours uniquement `/`, `/refonte`, `/mentions-legales`, `/cgv`, `/cgu`, 404. Aucun `/en/*`, aucun `/parrainage`, aucun formulaire fusionné (`ContactForm`, `TestimonialForm`, `ReferralLeadFormEn`).
- **`viewport.themeColor`** absent de `app/layout.tsx` et `app/en/layout.tsx` — signalé cycles 08-02 + 08-09, non transformé en issue (attend décision couleur brand exacte, probablement `#5B3FD4`).
- **Tap-targets sociaux 38×38 px** (`styles.css:2130-2141`) — signalé cycle 08-09, non transformé en issue. WCAG 2.5.5 AAA + 2.5.8 AA (min 24×24) recommandent 44×44.

**Points ouverts nouveaux ce cycle :**
- **`.github/` hygiène** : toujours 0 template, 0 CODEOWNERS, 0 dependabot.yml — 2e cycle consécutif sans action. À traiter dans une PR d'infra dédiée (effort S).
- **Taxonomie labels non documentée** : les labels priority/dimension/market/agent/type créés par #156 ne sont documentés ni dans README, ni dans CONTRIBUTING. Le prochain agent qui crée une issue ne connaît pas la convention.
- **README.md obsolète** : ligne 23 dit `test:unit # Vitest (lib/**)` — obsolète depuis PR #178 (Vitest couvre aussi composants React). Lignes 50-56 : la liste des jobs CI omet le job a11y.
- **CLAUDE.md:27 obsolète** : mentionne `a11y/ (axe-core — non branché en CI, voir §12)`, mais le job a11y tourne dans `.github/workflows/ci.yml:98-124` depuis PR #167.
- **`package.json:20` script `"ci"`** : `typecheck && lint && build` — n'inclut ni `test:unit`, ni `test:e2e`, ni `test:a11y`. Faux positif possible pour un dev qui fait `npm run ci` avant push.
- **Memory bank** (`activeContext.md`, `progress.md`) datée 2026-08-12, à rafraîchir post-audit 2026-08-16 (comme prévu dans le brief).
- **Divergence `getNotionClient()` triplée** : `lib/notion.ts:29-35` (throw), `lib/notion-referrals.ts:23-34` (return null + warn), `lib/notion-referrals-us.ts:15-21` (return null silencieux). Un helper unique (`lib/notion-client.ts`) centraliserait la sémantique. (Finding P2 Pôle Engineering.)
- **`PROVIDER_US` adopté partiellement** : `lib/organization.ts` (nouveau, 13 lignes) importé par 4 pages seulement — ~15 autres pages `/en/*` avec `areaServed: 'United States'` inlinent encore leur propre `provider: { Organization ... }` sans `email`. (Finding P2 Pôle Engineering.)
- **`dynamic({ ssr: false })` en Server Component** : `app/parrainage/page.tsx:8-11` + `app/en/parrainage/page.tsx:9-12`. Next.js 14 tolère, Next.js 15+ rend illégal — friction supplémentaire pour la future migration. (Finding P2 Pôle Engineering.)
- **`.section-dark` reliquat dark theme** : `styles.css:232` définit `.section-dark { background: var(--bg-dark); }` — utilisé par `/en/pricing:234`, `/en/about:103`, `/en/page.tsx:888`, `/en/reviews:108`. PR #175 a remplacé le background sur d'autres classes, mais celle-ci reste. (Finding P2 Pôle Product.)
- **`--text-subtle: #9C9892`** sur `--bg: #FAF8F4` (`styles/globals.css:13,280`) : ratio ~2.7:1, échoue WCAG 1.4.3 AA (4.5:1). Usage limité (cookie banner microcopy) mais présent sur un composant légal universel. (Finding P2 Pôle Product.)
- **`.audit-cta-btn` utilise `var(--purple)` (#8b5cf6)** au lieu de `var(--primary)` (#5B3FD4) sur la Home FR AuditGratuit. Inconsistance brand + risque de régression si `--purple` disparaît. (Finding P2 Pôle Product.)
- **`<main>` landmark absent** sur `/parrainage` FR/EN + `/temoignage` (documenté dans `SkipLink.tsx:8-14` comme fallback `<h1>`). WCAG 1.3.1 + 2.4.1. Effort XS. (Finding P2 Pôle Product.)
- **`ReferralLeadFormEn` sans aria-invalid live** : `checkValidity()` seulement au submit, pas de `handleBlur`/`handleInput` — divergence vs pattern `ContactForm.tsx:160-172`. (Finding P2 Pôle Product.)
- **HeaderEn ne link pas `/en/parrainage`** : la nav EN (`HeaderEn.tsx:57-66`) n'expose pas la page US référence, contrairement à `Header.tsx:57-65` FR. Cache le programme aux visiteurs organiques US. (Finding P1 Pôle Product — non transformé en issue, à traiter avec #181.)
- **`/en/parrainage/layout.tsx` dead imports + CSS scoping** : `HeaderEn` et `Footer` importés mais jamais rendus (retour `<>{children}</>`), et `parrainage.css` importée uniquement par `app/parrainage/layout.tsx` FR — donc les règles `.prrn-page …` peuvent ne pas s'appliquer à `/en/parrainage`. À vérifier en build prod. (Finding P1 Pôle Product — non transformé en issue, à traiter avec #181.)
- **3 audiences distinctes sous `/en/*`** : US contractors (`/en/pricing`, sectorielles), European anglophones (`/en/web-agency-*` = 8 pages géo FR traduites, NAP FR volontairement conservé), parrainage hybride (`/en/parrainage`). À documenter explicitement dans `.claude/memory/activeContext.md` pour éviter que le prochain audit ne re-flag `LocalBusiness FR NAP sur /en/*` comme un bug. (Finding P3 Pôle Growth.)
- **Aucune section publique de comparaison** vs Simplébo/Wix (FR) ni vs Housecall Pro/ServiceTitan/Thumbtack (US). Occasion manquée : c'est le seul angle où 199 €+Option Visibilité et Starter $499+Care $79/mo se défendent chiffres-à-chiffres. (Finding P2 Pôle Business — voir Opportunités.)
- **Vocabulaire "SME/craftsperson" utilisé côté /en/parrainage** vs "contractor" ailleurs (`/en/pricing`, `/en/faq`, `/en/contractor-websites`). Confond deux publics US. À traiter avec #181. (Finding P2 Pôle Marketing.)
- **`Testimonials.tsx` fallback à 1 seul témoignage** : le pipeline Notion (#164) est branché, mais tant que la base n'a pas ≥3 avis publiés, la section Home FR reste anémique.
- **Études de cas FR** : 0/4 avec verbatim client cité, 0/4 avec chiffre mesuré. Bloque le Trust Score. Contrainte : accord client (humain).
- **Case studies US** : 0 client réel, uniquement 2 « concept demos » clairement labellisés. Nécessite un 1er client US.

---

## Table de correspondance findings → issues créées

| Finding | Pôle(s) | Issue |
|---|---|---|
| NAP Home FR Contact `07/06` incohérent | Product P0-PROD-1 | **#180** |
| `/en/parrainage` marketing body toujours EUR (follow-up #155) | Growth G-P0-01 · Business BI-P0-1 · Product P1-PROD-1 · Marketing P0-MKT-01+02 | **#181** |
| UTM tracking silencieusement strip par `LeadSchema` Zod | Business BI-P1-1 | **#182** |
| `ReferralLeadFormEn` sans event GTM/Vercel Analytics | Operations P1-OPS-01 | **#183** |
| Scripts `site-vitrine` alias mort | Business BI-P1-2 | **#184** |
| Sitemap `lastModified: new Date()` bruyant (79 URLs) | Growth G-P1-01 · Operations P2-OPS-08 | **#185** |

**Non transformés en issues** (documentés en "Points ouverts" ci-dessus, à ouvrir sur décision humaine) :
- HeaderEn ne link pas `/en/parrainage` (à traiter avec #181)
- `/en/parrainage/layout.tsx` dead imports + CSS scoping (à traiter avec #181)
- Tap-targets sociaux 38×38 px (3e cycle sans action)
- `viewport.themeColor` absent (3e cycle sans action)
- `.github/` templates/CODEOWNERS/dependabot (2e cycle sans action)
- FAQ FR 7 vs 12 EN (recurring)
- `humans.txt` absent (recurring)
- `llms.txt` 63/120 lignes (progression partielle)
- Blog FR = 0 articles (recurring)
- Case studies FR sans verbatim/chiffres (bloqué décision humaine)
- Case studies US = 2 concept demos (bloqué 1er client US)
- Section « Vs alternatives » absente FR + US
- CSP `unsafe-inline` (dette assumée, chemin technique hash SHA-256 documenté sur #150)
- Next.js 14 → 15/16 (dette assumée, migration breaking planifiée séparément)

---

*Aucun chiffre n'a été inventé dans ce rapport. Tout KPI marqué "non mesuré" attend une source analytics live à brancher.
Vérifications indépendantes des findings P0/P1 effectuées avant création d'issue (grep `€/EUR` sur /en/parrainage = 34, comparaison numéros tel/display Contact.tsx, absence GTM ReferralLeadFormEn, LeadSchema sans utm_*, hits `site-vitrine` scripts, `lastModified: new Date()` sitemap).*
