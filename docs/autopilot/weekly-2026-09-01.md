# PIXELORIA WEEKLY REPORT — 2026-09-01

> Rapport hebdomadaire de la Pixeloria AI Team.
> Cycle : 2026-08-23 → 2026-09-01 (9 jours).
> Auteur : CEO AI · synthèse des 6 pôles (Growth · Product · Engineering · Marketing · Business · Operations).
> Mode d'exécution : scheduled task non-interactive, audit lecture-seule, aucun code du site modifié (seuls ce rapport + l'issue #191 ont été produits).

---

## Résumé exécutif

**La vélocité de delivery est revenue et le backlog a été entièrement soldé.** Le blocage n°1 du rapport précédent — PR #186 en draft depuis 6 jours — a été **mergé** (arrivé sur `main` le 2026-08-17, `d625738`), et la vague de corrections du **2026-08-23/24** a **fermé les 6 issues ouvertes (#180 → #185)**. Le backlog GitHub est passé de **6 issues ouvertes → 0**. La boucle observer→apprendre AutoPilot, à l'arrêt depuis 3 semaines, a été **réactivée** : `docs/autopilot/experiments.md` est passé de vide → 26 lignes.

**Santé technique du repo : verte.** `tsc --noEmit` propre, `next lint` sans warning, **58/58 tests unitaires passent**, `next build` réussit (First Load JS partagé : 87,4 kB). `main` et la branche de travail sont alignés (0/0).

**Deux réserves persistent, dont une désormais tracée :**
1. **Next.js 14.2.35 conserve 5 advisories HIGH** (`npm audit` : DoS/SSRF/XSS/cache poisoning côté `next` + transitives `postcss`/`glob`). Découverte récurrente depuis 2026-08-12 mais jamais tracée — **matérialisée ce cycle en issue #191** (aucun doublon préexistant, vérifié). La migration reste une décision produit (breaking).
2. **La seconde moitié du cycle est de nouveau silencieuse** : après le 2026-08-24, **7 jours sans commit** (2026-08-25 → 2026-09-01). Le sprint a été concentré sur 2 jours puis s'est arrêté.

Note de nuance sécurité : l'issue #150 (retrait `'unsafe-inline'` de la CSP `script-src`) est **clôturée**, mais `next.config.mjs:35` contient toujours `script-src 'self' 'unsafe-inline' …` — clôture apparente sans retrait effectif. Surface XSS résiduelle documentée ci-dessous.

## Scores

Barème d'estimation qualitatif — toujours **aucun accès analytics live** dans ce cycle. Les deltas vs 2026-08-23 reflètent des changements structurels observables (backlog soldé, PR mergée, fixes vérifiés au code).

| Axe | Score | Δ vs 2026-08-23 | Note |
|---|---|---|---|
| Business | 66/100 | +2 | `/en/parrainage` recopié en USD (#181), tunnel US désormais mesuré |
| SEO | 77/100 | +3 | `sitemap.lastModified` réel par page (#185) + og:image FR ajouté |
| GEO | 63/100 | +1 | `llms.txt` 63 → **73 lignes** (+10 pages locales) — cible ~120 |
| AI Citation | 55/100 | 0 | Aucun outil monitoring branché |
| Conversion (CRO) | 62/100 | +7 | PR #186 mergée (silent lead loss + phone corrigés), TestimonialForm validation/tracking |
| UX | 63/100 | +1 | Corrections funnel, pas d'évolution structurelle majeure |
| Performance | 72/100 | 0 | Non mesuré Lighthouse ; build OK, First Load partagé 87,4 kB |
| Brand / Trust | 51/100 | +1 | 0 nouvel avis intégré ; pipeline testimonials fiabilisé |
| Accessibilité | 78/100 | 0 | Aucune régression (lint clean), aucun ajout ciblé |
| Sécurité | 80/100 | −2 | Hardening code livré (#186 URL validation), mais 5 CVE HIGH Next.js persistent (#191) + CSP `unsafe-inline` non retiré (#150) |
| Qualité des données (CRM/Notion) | 60/100 | +8 | UTM Zod réparé (#182), event GTM tunnel US (#183), alias mort purgé (#184) |

## Évolution de la semaine

| KPI | Valeur | Note |
|---|---|---|
| Trafic organique | non mesuré | GSC / Vercel Analytics non branchés — 4ᵉ cycle consécutif |
| Impressions | non mesuré | idem |
| Clics | non mesuré | idem |
| Leads | non mesuré | Notion non exposé au pipeline AutoPilot |
| Taux de conversion | non mesuré | idem |
| Positions Google | non mesuré | GSC non branché |
| Citations IA (ChatGPT / Claude / Perplexity) | non mesuré | aucun outil de monitoring |
| Performances LCP / CLS / INP | non mesuré | Lighthouse à lancer en prod |
| Avis Google | non mesuré au repo | 0 nouvel avis intégré ce cycle |
| Backlinks | non mesuré | aucun outil de monitoring |
| Pages indexées (sitemap) | **79 URLs** — stable | aucune nouvelle page publiée |
| Nouvelles réalisations FR | **+0** | `app/realisations/` : 4 études, stable |
| Études de cas US | **0 réelles** | `app/en/case-studies/*/` = 0 sous-dossiers — reste à combler |
| Articles publiés | **+0** | pas de section blog FR |
| Temps de chargement moyen | non mesuré | Performance Guardian à programmer |
| Commits sur `main` (cycle) | **8** (6 fixes/features + 2 docs) | tous datés 2026-08-23/24 |
| Commits sur `main` (7 derniers jours) | **0** | silence 2026-08-25 → 2026-09-01 |
| PR mergées (cycle) | **≥1** (#186 le 2026-08-17) + vague fixes 08-23 | retour de la vélocité |
| PR ouvertes | **0** | plus aucune PR en attente |
| Issues ouvertes (backlog) | **0** | **−6** vs cycle précédent — backlog soldé |
| Issues fermées ce cycle | **6** (#180-#185) + #150 (CSP, closed) | closed_at ~2026-08-23 |
| Issues créées ce cycle | **1** (#191 — Next.js CVE, ce rapport) | tracker sécurité manquant |
| llms.txt | **73 lignes** (+10) | cible ~120 pour saturer GEO |
| experiments.md (boucle AutoPilot) | **26 lignes** (était vide) | boucle observer→apprendre réactivée |
| Santé CI locale | typecheck ✅ · lint ✅ · 58/58 unit ✅ · build ✅ | vérifié 2026-09-01 |
| `npm audit` | **6 vulns (1 low, 5 HIGH)** | tracé en #191 |

## Actions réalisées

| Élément | État | Impact |
|---|---|---|
| **PR #186** — daily audit 2026-08-17 | **Mergée** (`d625738`) | 2 P0 corrigés (silent lead loss Tunnel/Creation + phone display mismatch), validation URL scheme, +9 pages locales llms.txt |
| **#181** — `/en/parrainage` corps en euros | **Fermée** (`c1cc398`) | Copy recopiée en USD, langage Stripe retiré — crédibilité US restaurée |
| **#182** — UTM tracking Zod strip | **Fermée** (`e83ce59`) | `LeadSchema` ne strippe plus `utm_*` — attribution campagne restaurée |
| **#183** — GTM event tunnel US | **Fermée** | `track('referral_lead_submit_en', …)` présent (`ReferralLeadFormEn`) — vérifié au code |
| **#184** — alias mort `site-vitrine` scripts | **Fermée** (`7dc6ee3`) | Scripts Notion/Stripe purgés du slug mort |
| **#185** — `sitemap.ts` `lastModified` | **Fermée** (`592e4f8`) | `lastModified` réel par page — signal de fraîcheur SEO propre |
| **#180** — Contact NAP phone mismatch | **Fermée** | Corrigé via PR #186 |
| og:image FR + TestimonialForm | Livré (`95ec9b1`, 2026-08-24) | og:image manquant ajouté au layout FR ; validation + tracking TestimonialForm |
| Rapports hebdo 2026-08-16 & 08-23 | Committés (`fcc653e`, `8a7bb77`) | Le rapport 08-16 « manquant » a été backfillé |
| `experiments.md` | Réamorcé (26 lignes) | Boucle observer→apprendre relancée |

## Résultats mesurés

- **KPI live toujours indisponibles** (limite documentée depuis 2026-07-27, 4ᵉ cycle consécutif) — aucun chiffre trafic/conversion réel.
- Résultats structurels mesurables — **tous positifs ce cycle** :
  - Backlog : **6 → 0 issue ouverte** (−100 %).
  - PR en attente : **1 draft → 0**.
  - Vélocité : **≥6 fixes + 1 PR P0 mergés** contre 0 le cycle précédent.
  - Santé code : **58/58 unit, build + typecheck + lint verts** (vérifié ce jour).
  - Dette d'observabilité AutoPilot : `experiments.md` **vide → 26 lignes**.

## Actions sans résultat suffisant

- **Migration Next.js** — 5 advisories HIGH toujours non résolues ; désormais tracée (#191) mais non planifiée. Décision produit en attente depuis 2026-08-12.
- **CSP `'unsafe-inline'`** — #150 clôturée sans retrait effectif dans `next.config.mjs` ; surface XSS résiduelle.
- **Branchement analytics live** — 4ᵉ cycle « non mesuré » sur tout KPI trafic.
- **Études de cas US** — toujours 0 sous-dossier concret dans `app/en/case-studies/`.

## Régressions

- **Aucune régression fonctionnelle détectée.** Suite unitaire verte (58/58), lint sans warning, build prod OK.
- **Régression opérationnelle (soft)** : après le pic de delivery des 2026-08-23/24, **7 jours de silence** (0 commit 08-25 → 09-01). Le rythme reste en dents de scie (sprint concentré puis arrêt), pas encore une cadence régulière.

---

## Opportunités

### Planifier la migration Next.js (P1 · Sécurité)
Cf. **#191** (créée ce cycle). 5 advisories HIGH persistantes. Étapes : `npm audit fix` (LOW immédiat) → dernière 14.2.x patchée (non-breaking) → décision 14→15/16 (breaking, E2E complet). *(Pôle Engineering — Security Guardian.)*
**Impact estimé** : Élevé (réduction de risque) — condition de conformité et de sérénité prod.

### Retirer effectivement `'unsafe-inline'` de la CSP `script-src` (P1 · Sécurité)
#150 est close mais `next.config.mjs:35` conserve `'unsafe-inline'`. Rouvrir le sujet via hash SHA-256 au build (compatible statique, chemin déjà proposé). *(Pôle Engineering.)*
**Impact estimé** : Moyen — durcit la surface XSS App Router.

### Brancher une source analytics live (GSC ou Vercel Analytics API)
Toujours ouvert depuis 4 cycles. Sans ce branchement, chaque rapport reste aveugle aux vrais KPI et aucune boucle A/B n'est possible.
**Impact estimé** : Élevé (indirect) — condition sine qua non de toute priorisation data-driven. Effort M.

### Enrichir `public/llms.txt` (73 → ~120 lignes)
Progresse (63 → 73) mais reste sous-alimenté. Ajouter : FAQ synthétique, verticales EN, études de cas, humans.txt. *(Pôle Marketing + Growth.)*
**Impact estimé** : Moyen — portée LLM directe. Effort S.

### Créer 2-3 vraies études de cas US
`app/en/case-studies/*/` = **0 sous-dossier concret**. Opportunité citée depuis 2026-08-09. *(Pôle Growth Intelligence US.)*
**Impact estimé** : Élevé pour Trust US — bloque des conversions contractors.

### Compléter la FAQ FR
Parité FR<EN persistante (FR reste plus légère que EN — non re-mesuré précisément ce cycle). *(Pôle Marketing.)*
**Impact estimé** : Moyen — enrichissement passif du `FAQPage` schema.

### Stabiliser la cadence de delivery
Le cycle a livré en 2 jours puis s'est arrêté 7 jours. Étaler le flux (au moins 1 petit merge/2-3 jours) fiabiliserait la boucle AutoPilot.
**Impact estimé** : Moyen (santé process) — réversible, mesurable via fréquence de commits.

## Top 5 des priorités (semaine 2026-09-01 → 2026-09-08)

1. **#191 — Migration Next.js / advisories HIGH** (P1 · Sécurité). *`npm audit fix` immédiat (LOW), puis planifier la cible 14.2.x/15/16. Seule vraie dette de risque restante.*
2. **Brancher analytics live** (GSC ou Vercel Analytics API). *4ᵉ cycle « non mesuré ». Débloque toute mesure ROI et la boucle apprentissage.*
3. **CSP — retrait effectif de `'unsafe-inline'`** (P1 · Sécurité). *#150 close mais non appliquée ; rouvrir via hash SHA-256 build.*
4. **Créer 2-3 études de cas US** (Trust US). *0 sous-dossier concret ; bloque des conversions contractors.*
5. **`llms.txt` 73 → ~120 lignes** + **FAQ FR** (GEO/Marketing). *Deux petits enrichissements de contenu à fort levier LLM/schema.*

Objectif de la semaine : **capitaliser sur le backlog à zéro** pour attaquer les 2 chantiers structurels laissés de côté (analytics live + migration Next.js), plutôt que de laisser le repo retomber en silence.

## Décisions humaines nécessaires

1. **Migration Next.js 14 → 15 ou 16** — planning + E2E avant/après. Seule dépendance HIGH non résolue (5 advisories). Tracée en **#191**. *(Persistant depuis 2026-08-12.)*
2. **Branchement d'au moins une source analytics live** (GSC ou Vercel Analytics API). *(Persistant depuis 2026-07-27, 4ᵉ cycle.)*
3. **CSP — confirmer le chemin hash SHA-256** pour retirer réellement `'unsafe-inline'` (le chemin nonces avait été abandonné pour régression Perf). #150 ayant été close sans retrait, décider : rouvrir ou acter le risque.
4. **Cadence de delivery** — valider un rythme cible (ex. au moins 1 merge tous les 2-3 jours) pour éviter les cycles en dents de scie.

## Plan de la semaine suivante

- **Semaine 2026-09-01 → 2026-09-08** : profiter du backlog vide pour lancer les 2 chantiers de fond — (a) `npm audit fix` + planning migration Next.js (#191), (b) branchement d'une source analytics live.
- **Alimenter `experiments.md`** : consigner au moins une hypothèse chiffrée mesurable dès qu'une source analytics est branchée (ex. impact du fix UTM #182 sur la complétude d'attribution Notion).
- **Prochain rapport hebdomadaire** : 2026-09-08 — mêmes 6 pôles, focus sur (a) démarrage effectif migration/analytics, (b) régularité de la cadence, (c) premiers KPI live si branchés.
- **Alerte à surveiller** : si aucun commit n'intervient d'ici le prochain rapport, le repo sera retombé en silence complet malgré un backlog vide — signal que l'énergie doit se reporter sur les chantiers structurels (analytics, Next.js) plutôt que sur le seul traitement de backlog.

---

## Points ouverts (non bloquants, à documenter)

- **Issue créée ce cycle** : #191 (Next.js advisories HIGH / migration). Vérification de doublon effectuée via `mcp__github__search_issues` (2 requêtes, 0 résultat) avant création. Aucune autre issue créée — le backlog étant à zéro, il n'y avait pas d'autre finding Critique/Important non tracé.
- **Correspondance findings ↔ issues (cycle précédent, toutes fermées ce cycle)** :

| Finding | Issue | État |
|---|---|---|
| Contact NAP phone mismatch | #180 | Fermée (via PR #186) |
| `/en/parrainage` corps euros | #181 | Fermée (`c1cc398`) |
| UTM Zod strip | #182 | Fermée (`e83ce59`) |
| GTM event tunnel US manquant | #183 | Fermée (event vérifié au code) |
| Scripts alias mort `site-vitrine` | #184 | Fermée (`7dc6ee3`) |
| `sitemap.ts` `lastModified` | #185 | Fermée (`592e4f8`) |
| CSP `'unsafe-inline'` script-src | #150 | Close **sans retrait effectif** — `next.config.mjs:35` conserve `'unsafe-inline'` |

- **Nuance CSP** : la clôture de #150 ne correspond pas à un retrait de `'unsafe-inline'` dans le code. À traiter comme un point de sécurité toujours actif (voir Décisions humaines #3).
- **Marché US** : la synthèse dédiée `/ceo-ai-us` reste à produire séparément ; les issues `market/us` (#181, #183) sont incluses ici par souci de vision unifiée.
- **`npm audit` détail** : `next` (5+ advisories HIGH), `postcss <=8.5.22` (HIGH, transitive), `glob 10.2.0-10.4.5` (HIGH, via `eslint-config-next`), `postcss-selector-parser 7.1.0-7.1.2` (LOW, fix non-breaking). Détail complet dans #191.

## Backlog complet à date

**0 issue ouverte.** **0 PR ouverte.** Backlog soldé au 2026-09-01. Seule issue active : **#191** (créée par ce rapport, P1 sécurité).

---

*Aucun chiffre n'a été inventé dans ce rapport. Tout KPI marqué « non mesuré » attend une source analytics live à brancher (voir Décisions humaines). Les scores sont des estimations qualitatives adossées à l'observable structurel du repo (backlog, commits, tests, `npm audit`), avec delta explicite vs le rapport 2026-08-23. La santé CI (typecheck/lint/58 tests/build) a été réellement exécutée le 2026-09-01.*
