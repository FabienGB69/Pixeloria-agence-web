# PIXELORIA WEEKLY REPORT — 2026-09-13

> Rapport hebdomadaire de la Pixeloria AI Team.
> Cycle : 2026-09-06 → 2026-09-13 (7 jours).
> Auteur : CEO AI · synthèse des 6 pôles (Growth · Product · Engineering · Marketing · Business · Operations).
> Mode d'exécution : scheduled task **non-interactive**, audit **lecture-seule**, **aucun code du site modifié** (seuls ce rapport et les issues #198/#199 ont été produits).

---

## Résumé exécutif

**✅ `main` est de nouveau VERT — la régression P0 du cycle précédent (#195) est corrigée, et la migration Next.js 15 est désormais effective en prod.**

Le chantier n°1 du rapport 2026-09-06 a été traité. La PR **#196** (`fc036c6`, mergée le 2026-09-07) a **aligné `package.json` sur `next: "^15.5.25"`** pour matcher le lockfile, réparant le désync qui cassait `npm ci` sur les 6 jobs CI depuis le 2026-09-02. **Vérifié sur 3 sources ce jour :**
1. `package.json` = `^15.5.25` **et** lockfile = `15.5.25` → cohérents.
2. Repro locale `npm ci --dry-run` → **succès** (641 paquets, exit 0), plus d'`EUSAGE`.
3. **GitHub Actions run #430** (`main`, head `d048ba4`, merge #196) = **success**. Dernier run PR (#431, #197) = success également.

**Conséquence sécurité majeure** : avec `next@15.5.25` réellement installé, `npm audit` confirme que les **5 advisories HIGH _runtime_ de `next`** (SSRF, DoS Server Components, cache poisoning, XSS CSP…) que #191 visait sont **éliminées**. `next` n'apparaît plus qu'en *moderate* (via `postcss`). **#191 et #195 sont clos.** Le backlog d'issues est repassé à **0 ouvert** en début de cycle.

**Trois réserves de fond, inchangées :**
1. **CSP `'unsafe-inline'` toujours présent** (`next.config.mjs:35`, `script-src`) — #150 clôturée sans retrait effectif (4ᵉ cycle signalé).
2. **PR #197 (docs) dort depuis 6 jours** : elle remplit le dernier critère de #191 (consigner la décision Next 15 dans `docs/autopilot/experiments.md`), mais reste **draft non-mergée** → `experiments.md` sur `main` est encore le template vide (« Aucune expérience enregistrée »). La boucle observer→apprendre AutoPilot n'a donc **toujours capitalisé aucun cycle**.
3. **Cadence en pic unique** : toute la delivery s'est concentrée le 2026-09-07 (fix + PR #197 ouverte), puis 6 jours calmes. Acceptable cette fois (main vert, backlog vide) — le seul fil qui pend est #197.

## Scores

Barème d'estimation **qualitatif** — toujours **aucun accès analytics live** (6ᵉ cycle consécutif). Les deltas vs 2026-09-06 reflètent des changements structurels observables (état CI, dépendances, contenu au repo). Aucun KPI trafic/conversion réel n'est disponible.

| Axe | Score | Δ vs 2026-09-06 | Note |
|---|---|---|---|
| Business | 66/100 | 0 | Aucun changement de contenu/offre landé ce cycle |
| SEO | 77/100 | 0 | Sitemap (~79 URLs) / metadata inchangés |
| GEO | 63/100 | 0 | `llms.txt` **stagne à 73 lignes** (cible ~120) — 0 progrès (4ᵉ cycle) |
| AI Citation | 55/100 | 0 | Aucun outil monitoring branché |
| Conversion (CRO) | 62/100 | 0 | Pas de changement funnel/formulaires ce cycle |
| UX | 63/100 | 0 | Aucune évolution de contenu/parcours |
| Performance | 74/100 | **+2** | **Next 15 désormais effectif en prod** (raison de blocage levée vs 09-06) — gain structurel, **non mesuré Lighthouse** |
| Brand / Trust | 51/100 | 0 | 0 nouvel avis, 0 étude de cas US (toujours 0 sous-dossier) |
| Accessibilité | 78/100 | 0 | Job a11y de nouveau exécutable (CI verte) mais **non re-audité numériquement** ce cycle |
| Sécurité | 84/100 | **+5** | **5 CVE HIGH runtime `next` éliminées** (Next 15 effectif en prod), main vert. Résiduel = transitifs build-time (voir #199). Plafonné par CSP `'unsafe-inline'` persistant (#150) |
| Qualité des données (CRM/Notion) | 60/100 | 0 | Aucun changement pipeline UTM/Notion ce cycle |
| **Santé delivery / CI** *(hors-barème)* | **VERT** | **⬆⬆** | **main réparé** (run #430 success), backlog issues 2 → 0 |

## Évolution de la semaine

| KPI | Valeur | Note |
|---|---|---|
| Trafic organique | non mesuré | GSC / Vercel Analytics non branchés — **6ᵉ cycle consécutif** |
| Impressions | non mesuré | idem |
| Clics | non mesuré | idem |
| Leads | non mesuré | Notion non exposé au pipeline AutoPilot |
| Taux de conversion | non mesuré | idem |
| Positions Google | non mesuré | GSC non branché |
| Citations IA (ChatGPT / Claude / Perplexity) | non mesuré | aucun outil de monitoring |
| Performances LCP / CLS / INP | non mesuré | Lighthouse à lancer en prod (Next 15 désormais actif → à re-mesurer) |
| Avis Google | non mesuré au repo | 0 nouvel avis intégré ce cycle |
| Backlinks | non mesuré | aucun outil de monitoring |
| Pages indexées (sitemap) | **79 URLs** — stable | `app/sitemap.ts` (79 `path:`), aucune nouvelle page |
| Réalisations FR | **4** (stable) | content-by-denise, escalee-gouts, lenora-conciergerie, qit-concierge |
| Études de cas US | **0 réelles** | `app/en/case-studies/` = `page.tsx` seul, 0 sous-dossier — inchangé depuis 2026-08-09 |
| Articles publiés | **+0** | pas de section blog FR |
| Temps de chargement moyen | non mesuré | Performance Guardian à programmer |
| Commits `main` (cycle, hors merges) | **1** | `fc036c6` (fix deps #196) |
| PR mergées (cycle) | **1** (#196) le 2026-09-07 | Corrige la régression P0 #195 |
| PR ouvertes | **1** (#197, draft, docs) | Ouverte le 2026-09-07, non-mergée depuis **6 j** |
| Issues ouvertes (début de cycle) | **0** | #191 + #195 clos via #196 |
| Issues créées ce cycle | **2** (#198 Ops, #199 Security) | 0 doublon (vérifié `search_issues`) |
| llms.txt | **73 lignes** (stable) | 0 progrès, cible ~120 |
| **CI `main` (dernier run #430)** | **SUCCESS** (2026-09-07) | 6 jobs verts, désync résolu |
| `npm ci --dry-run` local | **succès** (exit 0) | `next@15.5.25` == `^15.5.25` |
| `npm audit` | **7 vulns (5 mod, 2 high)** | 5 HIGH runtime `next` **éliminées** ; résiduel = transitifs build-time (#199) |

## Actions réalisées

| Élément | État | Impact |
|---|---|---|
| **#195 — Réparer `main` / `npm ci`** | **Corrigé** (PR #196, `fc036c6`) | `package.json` → `^15.5.25`, CI verte (run #430). P0 clos. |
| **#191 — Migration Next.js 15** | **Effective en prod, close** | Next 15.5.25 réellement installé/déployé ; 5 advisories HIGH runtime clos |
| **PR #196** — fix deps + rapport 09-06 | Mergée (run #430 success) | A débloqué CI + déploiement |
| **Issue #198** (ce rapport) | Créée (P1, Ops) | Garde-fou : rendre le CI *required* sur `main` (anti-récidive #195) |
| **Issue #199** (ce rapport) | Créée (P1, Security) | Suivi `npm audit` : js-yaml HIGH (fix non-breaking) + postcss→next@16 (breaking) |

## Résultats mesurés

- **KPI live toujours indisponibles** (limite documentée depuis 2026-07-27, 6ᵉ cycle) — aucun chiffre trafic/conversion réel.
- Résultats structurels mesurables ce cycle :
  - **CI `main` : ROUGE (#427) → VERT (#430)** — désync `package.json`↔lockfile résolu, vérifié sur 3 sources.
  - **Migration Next 15 effective en prod** — `npm audit` confirme la disparition des 5 advisories HIGH _runtime_ de `next` (posture sécurité runtime nettement améliorée).
  - **Backlog issues : 2 → 0** (ouvert), puis **0 → 2** (#198/#199 créées ce rapport — process/dette, non bloquantes).

## Actions sans résultat suffisant

- **PR #197 (docs experiments.md)** — ouverte le 2026-09-07, **toujours draft non-mergée** : `experiments.md` sur `main` reste vide. La boucle observer→apprendre AutoPilot n'a capitalisé **aucun** cycle (3ᵉ cycle ce constat).
- **CSP `'unsafe-inline'`** — toujours présent (`next.config.mjs:35`) ; #150 close sans retrait effectif. 4ᵉ cycle sans action.
- **Branchement analytics live** — 6ᵉ cycle « non mesuré » sur tout KPI trafic.
- **llms.txt** — stagne à 73 lignes (0 progrès), cible ~120.
- **Études de cas US** — toujours 0 sous-dossier concret dans `app/en/case-studies/`.

## Régressions

- **Aucune régression ce cycle.** La régression P0 #195 du cycle précédent est **corrigée** (CI verte). Aucun nouvel échec CI, aucune perte de contenu ni de fonctionnalité détectée.

---

## Opportunités

### Merger la PR #197 (docs) pour clore la boucle #191 (P2 · Docs) — Effort XS
`experiments.md` sur `main` est encore le template vide. Merger #197 consigne enfin la décision Next 15.5.25 (hypothèse, résultat, incident #195, enseignement) → première entrée réelle de la mémoire AutoPilot.
**Impact estimé** : Moyen (capitalisation process, débloque la boucle observer→apprendre). Effort XS.

### Rendre le CI un *required status check* sur `main` (P1 · Ops) — #198
#194 a pu être mergée en état cassé → 4 jours de main rouge. Activer la branch protection avec les 6 jobs CI *required* empêche la récidive.
**Impact estimé** : Élevé (prévention d'un P0). Effort S.

### `npm audit fix` non-breaking (P1 · Sécurité) — #199
Clôt le HIGH `js-yaml` (GHSA-2883-xcg3-v3hh) + moderates corrigeables **sans** changement majeur. Le HIGH `postcss` résiduel nécessite `next@16` (breaking) → à traiter comme migration dédiée.
**Impact estimé** : Moyen (réduction surface build-time). Effort S.

### Retirer effectivement `'unsafe-inline'` de la CSP `script-src` (P1 · Sécurité)
#150 close mais `next.config.mjs:35` conserve `'unsafe-inline'`. Next 15 facilite l'approche hash/nonce côté App Router — chemin hash SHA-256 au build documenté (compatible rendu statique).
**Impact estimé** : Moyen — durcit la surface XSS.

### Brancher une source analytics live (GSC ou Vercel Analytics API)
Ouvert depuis 6 cycles. Sans ce branchement, chaque rapport reste aveugle aux vrais KPI et aucune boucle A/B n'est possible.
**Impact estimé** : Élevé (indirect) — condition de toute priorisation data-driven. Effort M.

### Enrichir `public/llms.txt` (73 → ~120 lignes) + créer 2-3 études de cas US
Deux chantiers contenu stagnants (portée LLM directe ; Trust US bloque des conversions contractors).
**Impact estimé** : Moyen à Élevé. Effort S à M.

## Top 5 des priorités (semaine 2026-09-13 → 2026-09-20)

1. **Merger PR #197** — clore la boucle #191 et démarrer enfin la mémoire `experiments.md`. *Effort XS, aucun risque (docs-only, CI verte).*
2. **#198 — CI *required* sur `main`** — garde-fou anti-récidive du P0 #195. *Config repo, pas de code.*
3. **#199 — `npm audit fix` non-breaking** — clore le HIGH `js-yaml` + moderates ; planifier `next@16` séparément pour `postcss`.
4. **Brancher analytics live** (GSC ou Vercel Analytics API). *6ᵉ cycle « non mesuré » ; débloque toute mesure ROI, et permettrait de mesurer le gain Next 15 (LCP/INP).*
5. **CSP — retrait effectif de `'unsafe-inline'`** (#150) + **`llms.txt` 73 → ~120** + **études de cas US**. *Chantiers de fond, maintenant que main est stable.*

Objectif de la semaine : **capitaliser sur un main enfin vert et stable** — fermer les 2 loose ends immédiats (#197, #198), amorcer une source analytics, puis reprendre les chantiers de fond (CSP, contenu GEO/US) sur base saine.

## Décisions humaines nécessaires

1. **Merger PR #197** (docs-only, CI verte) — valider et fusionner pour clore la traçabilité #191.
2. **Politique de merge** — rendre le CI *required* avant merge sur `main` (branch protection, #198). Décision de config repo.
3. **Branchement d'au moins une source analytics live** (GSC ou Vercel Analytics API). *(Persistant depuis 2026-07-27, 6ᵉ cycle — devient le principal frein à toute priorisation data-driven.)*
4. **CSP** — confirmer le chemin (hash SHA-256 au build) pour retirer réellement `'unsafe-inline'` (#150 close sans retrait).
5. **Fenêtre migration `next@16`** — pour clore le HIGH `postcss` résiduel (#199), à planifier avec tests E2E (pas de `npm audit fix --force`).

## Plan de la semaine suivante

- **Semaine 2026-09-13 → 2026-09-20** : (a) merger #197, (b) activer le garde-fou CI #198, (c) `npm audit fix` non-breaking #199, (d) si possible amorcer le branchement analytics.
- **Alimenter `experiments.md`** : la fusion de #197 pose la 1ʳᵉ entrée (migration Next 15) ; ajouter une hypothèse chiffrée dès qu'une source analytics est branchée.
- **Prochain rapport hebdomadaire** : 2026-09-20 — focus sur (a) #197 mergée / mémoire démarrée, (b) garde-fou CI en place, (c) posture `npm audit` post-fix, (d) premiers KPI live si branchés.
- **Alerte à surveiller** : régularité de la cadence (éviter le pic unique + silence) et non-récidive d'un désync lockfile.

---

## Points ouverts (non bloquants, à documenter)

- **Issues créées ce cycle** : **#198** (P1, Ops — CI *required*) et **#199** (P1, Security — npm audit). Doublon vérifié via `search_issues` (0 résultat) avant chaque création. Les autres findings (CSP #150, llms.txt, études de cas US) sont déjà connus/tracés ou de faible sévérité — pas de nouveau Critique non tracé, pas de nouvelle issue superflue.
- **PR #197** laissée telle quelle par cette exécution (audit-only) — recommandée à la fusion humaine (voir Décisions).
- **Correspondance findings ↔ issues** :

| Finding | Issue / PR | État |
|---|---|---|
| main rouge / lockfile désync (#195) | #195 | **Close** — corrigé par #196 (`fc036c6`) |
| Migration Next 15 / advisories HIGH runtime | #191 | **Close** — effective en prod, 5 HIGH runtime éliminées |
| CI non *required* → merge d'un état cassé possible | **#198** | **Ouverte (P1)** — créée ce rapport |
| `npm audit` : js-yaml HIGH + postcss→next@16 | **#199** | **Ouverte (P1)** — créée ce rapport |
| Décision Next 15 non consignée / experiments.md vide | **PR #197** | **Ouverte (draft, 6 j)** — à merger |
| CSP `'unsafe-inline'` script-src | #150 | Close **sans retrait effectif** — `next.config.mjs:35` conserve `'unsafe-inline'` |

- **Détail `npm audit`** (mesuré ce jour, lockfile `next@15.5.25`) : 7 vulnérabilités — 2 HIGH (`js-yaml` via outillage lint, `postcss` via chaîne `next build`) + 5 moderate (`colord` stylelint, `next` via postcss, `vitest`/`@vitest/mocker`/`@vitest/coverage-v8` tests). **Toutes transitives et build/dev-time** — aucune exposée au visiteur en runtime. Les 5 advisories HIGH _runtime_ `next` du cycle précédent sont **éliminées**.
- **Santé CI locale** : `npm ci --dry-run` = succès. Décompte unitaire repris du correctif #196 (58/58 unit verts en CI, non re-décompté ici — l'audit reste lecture-seule sans réinstall complète).
- **Marché US** : la synthèse dédiée `/ceo-ai-us` reste à produire séparément.

## Backlog complet à date

**2 issues ouvertes** : **#198** (P1, Ops — garde-fou CI) et **#199** (P1, Security — npm audit hygiene). **1 PR ouverte** : **#197** (draft, docs — à merger). **0 P0.**

---

*Aucun chiffre n'a été inventé dans ce rapport. Tout KPI marqué « non mesuré » attend une source analytics live (voir Décisions humaines). Les scores sont des estimations qualitatives adossées à l'observable structurel du repo (état CI, dépendances, contenu), avec delta explicite vs le rapport 2026-09-06. La réparation de `main` (#195→#196) et l'élimination des 5 advisories HIGH runtime ont été vérifiées sur 3 sources indépendantes (état de `origin/main`, `npm ci --dry-run` local, run GitHub Actions #430 + `npm audit`).*
